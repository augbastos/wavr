{
  "name": "wavr-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Wavr Desktop — a Tauri v2 native shell around the loopback central (ADR-0007)",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "icon": "tauri icon ../frontend/icon.svg"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
